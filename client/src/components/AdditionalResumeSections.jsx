import { Plus, Trash2 } from 'lucide-react';

const inputClass = 'w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm font-medium text-slate-900 outline-none focus:border-orange-500';

const AdditionalResumeSections = ({ resume, handleArrayChange, addItem, removeItem, setResume }) => {
  const updateInterests = (value) => setResume((previous) => ({ ...previous, interests: value.split(',').map((item) => item.trim()).filter(Boolean) }));
  return <div className="space-y-7"><section><div className="mb-3 flex items-center justify-between"><div><h4 className="font-black text-slate-900">Achievements</h4><p className="text-xs text-slate-500">Awards and outcomes you can verify.</p></div><button type="button" onClick={() => addItem('achievements', { name: '', description: '', date: '' })} className="rounded-xl bg-orange-50 px-3 py-2 text-xs font-black text-orange-700"><Plus className="mr-1 inline h-3.5 w-3.5" /> Add</button></div>{resume.achievements?.map((item, index) => <div key={`achievement-${index}`} className="relative mb-3 grid gap-3 rounded-2xl bg-slate-50 p-4 sm:grid-cols-2"><button type="button" onClick={() => removeItem('achievements', index)} className="absolute right-3 top-3 text-slate-400 hover:text-red-500" aria-label="Remove achievement"><Trash2 className="h-4 w-4" /></button><input value={item.name || ''} onChange={(event) => handleArrayChange('achievements', index, 'name', event.target.value)} placeholder="Achievement or award" className={inputClass} /><input value={item.date || ''} onChange={(event) => handleArrayChange('achievements', index, 'date', event.target.value)} placeholder="Date" className={inputClass} /><textarea value={item.description || ''} onChange={(event) => handleArrayChange('achievements', index, 'description', event.target.value)} placeholder="Optional context" rows={2} className="sm:col-span-2" /></div>)}</section><section><div className="mb-3 flex items-center justify-between"><div><h4 className="font-black text-slate-900">Languages</h4><p className="text-xs text-slate-500">Add languages and proficiency honestly.</p></div><button type="button" onClick={() => addItem('languages', { name: '', proficiency: '' })} className="rounded-xl bg-orange-50 px-3 py-2 text-xs font-black text-orange-700"><Plus className="mr-1 inline h-3.5 w-3.5" /> Add</button></div>{resume.languages?.map((item, index) => <div key={`language-${index}`} className="mb-3 flex gap-2"><input value={item.name || ''} onChange={(event) => handleArrayChange('languages', index, 'name', event.target.value)} placeholder="Language" className={inputClass} /><input value={item.proficiency || ''} onChange={(event) => handleArrayChange('languages', index, 'proficiency', event.target.value)} placeholder="Proficiency" className={inputClass} /><button type="button" onClick={() => removeItem('languages', index)} className="rounded-xl p-2 text-slate-400 hover:bg-red-50 hover:text-red-500" aria-label="Remove language"><Trash2 className="h-4 w-4" /></button></div>)}</section><section><div className="mb-3 flex items-center justify-between"><div><h4 className="font-black text-slate-900">Volunteer Experience</h4><p className="text-xs text-slate-500">Community, nonprofit, or campus leadership work.</p></div><button type="button" onClick={() => addItem('volunteer', { role: '', organization: '', startDate: '', endDate: '', description: '' })} className="rounded-xl bg-orange-50 px-3 py-2 text-xs font-black text-orange-700"><Plus className="mr-1 inline h-3.5 w-3.5" /> Add</button></div>{resume.volunteer?.map((item, index) => <div key={`volunteer-${index}`} className="relative mb-3 grid gap-3 rounded-2xl bg-slate-50 p-4 sm:grid-cols-2"><button type="button" onClick={() => removeItem('volunteer', index)} className="absolute right-3 top-3 text-slate-400 hover:text-red-500" aria-label="Remove volunteer entry"><Trash2 className="h-4 w-4" /></button>{[['role', 'Role'], ['organization', 'Organization'], ['startDate', 'Start date'], ['endDate', 'End date']].map(([field, placeholder]) => <input key={field} value={item[field] || ''} onChange={(event) => handleArrayChange('volunteer', index, field, event.target.value)} placeholder={placeholder} className={inputClass} />)}<textarea value={item.description || ''} onChange={(event) => handleArrayChange('volunteer', index, 'description', event.target.value)} placeholder="What did you do?" rows={3} className="sm:col-span-2" /></div>)}</section><section><label className="mb-2 block font-black text-slate-900">Interests</label><input value={(resume.interests || []).join(', ')} onChange={(event) => updateInterests(event.target.value)} placeholder="Reading, public speaking, trekking" className={inputClass} /></section><section><div className="mb-3 flex items-center justify-between"><div><h4 className="font-black text-slate-900">Custom Sections</h4><p className="text-xs text-slate-500">Add relevant information that does not fit elsewhere.</p></div><button type="button" onClick={() => addItem('customSections', { title: 'Additional Information', content: '' })} className="rounded-xl bg-orange-50 px-3 py-2 text-xs font-black text-orange-700"><Plus className="mr-1 inline h-3.5 w-3.5" /> Add</button></div>{resume.customSections?.map((item, index) => <div key={`custom-${index}`} className="relative mb-3 rounded-2xl bg-slate-50 p-4"><button type="button" onClick={() => removeItem('customSections', index)} className="absolute right-3 top-3 text-slate-400 hover:text-red-500" aria-label="Remove custom section"><Trash2 className="h-4 w-4" /></button><input value={item.title || ''} onChange={(event) => handleArrayChange('customSections', index, 'title', event.target.value)} placeholder="Section title" className={`${inputClass} mb-3 pr-8`} /><textarea value={item.content || ''} onChange={(event) => handleArrayChange('customSections', index, 'content', event.target.value)} placeholder="Add your content" rows={4} className="w-full rounded-xl border border-slate-200 bg-white p-3 text-sm outline-none focus:border-orange-500" /></div>)}</section></div>;
};

export default AdditionalResumeSections;
