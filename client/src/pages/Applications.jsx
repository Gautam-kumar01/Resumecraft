import { useEffect, useMemo, useState } from 'react';
import { Link } from 'react-router-dom';
import SEO from '../components/SEO';
import { CalendarDays, ExternalLink, Plus, Save, Trash2 } from 'lucide-react';

const statuses = ['Saved', 'Applied', 'Interview', 'Offer', 'Rejected'];
const blank = { company: '', jobTitle: '', dateApplied: '', status: 'Saved', jobUrl: '', notes: '' };
const inputClass = 'w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm font-medium text-slate-900 outline-none focus:border-orange-500';

const Applications = () => {
  const [applications, setApplications] = useState(() => { try { return JSON.parse(localStorage.getItem('resumecraft_applications') || '[]'); } catch { return []; } });
  const [form, setForm] = useState(blank);
  const [filter, setFilter] = useState('All');
  useEffect(() => { localStorage.setItem('resumecraft_applications', JSON.stringify(applications)); }, [applications]);
  const visible = useMemo(() => filter === 'All' ? applications : applications.filter((item) => item.status === filter), [applications, filter]);
  const addApplication = (event) => { event.preventDefault(); if (!form.company.trim() || !form.jobTitle.trim()) return; setApplications((previous) => [{ ...form, id: crypto.randomUUID?.() || `${Date.now()}` }, ...previous]); setForm(blank); };
  const update = (id, field, value) => setApplications((previous) => previous.map((item) => item.id === id ? { ...item, [field]: value } : item));
  const remove = (id) => setApplications((previous) => previous.filter((item) => item.id !== id));

  return <div className="min-h-screen bg-slate-50"><SEO title="Application Tracker" description="Track saved jobs, applications, interviews, offers, and notes in one simple workspace." noindex /><main className="mx-auto max-w-6xl px-4 py-12"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-500">Career tools</p><h1 className="mt-2 text-4xl font-black tracking-tight text-slate-950">Application tracker</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-600">Keep your job search organized. Entries are saved locally on this device, so do not use this page for sensitive information you do not want stored in the browser.</p></div><Link to="/editor" className="rounded-xl bg-slate-950 px-4 py-3 text-sm font-black text-white hover:bg-orange-500">Create a resume</Link></div><form onSubmit={addApplication} className="mt-10 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center gap-2"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-orange-100 text-orange-600"><Plus className="h-4 w-4" /></span><h2 className="font-black text-slate-950">Add an application</h2></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><input required value={form.company} onChange={(event) => setForm({ ...form, company: event.target.value })} placeholder="Company" className={inputClass} /><input required value={form.jobTitle} onChange={(event) => setForm({ ...form, jobTitle: event.target.value })} placeholder="Job title" className={inputClass} /><input type="date" value={form.dateApplied} onChange={(event) => setForm({ ...form, dateApplied: event.target.value })} className={inputClass} /><select value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value })} className={inputClass}>{statuses.map((status) => <option key={status}>{status}</option>)}</select><input type="url" value={form.jobUrl} onChange={(event) => setForm({ ...form, jobUrl: event.target.value })} placeholder="Job URL (optional)" className={inputClass} /><input value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} placeholder="Notes (optional)" className={inputClass} /></div><button type="submit" className="mt-4 inline-flex items-center rounded-xl bg-orange-500 px-4 py-3 text-sm font-black text-white hover:bg-orange-600"><Save className="mr-2 h-4 w-4" /> Save application</button></form><div className="mt-8 flex flex-wrap items-center gap-2">{['All', ...statuses].map((status) => <button type="button" key={status} onClick={() => setFilter(status)} className={`rounded-full px-4 py-2 text-xs font-black ${filter === status ? 'bg-slate-950 text-white' : 'bg-white text-slate-600 ring-1 ring-slate-200 hover:ring-orange-300'}`}>{status}{status === 'All' ? ` (${applications.length})` : ` (${applications.filter((item) => item.status === status).length})`}</button>)}</div><section className="mt-5 space-y-3">{visible.length ? visible.map((item) => <article key={item.id} className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="grid gap-4 lg:grid-cols-[1fr_180px_170px_auto] lg:items-center"><div><input value={item.jobTitle} onChange={(event) => update(item.id, 'jobTitle', event.target.value)} className="w-full border-0 p-0 text-lg font-black text-slate-950 outline-none" /><input value={item.company} onChange={(event) => update(item.id, 'company', event.target.value)} className="mt-1 w-full border-0 p-0 text-sm font-bold text-slate-500 outline-none" />{item.dateApplied && <p className="mt-2 flex items-center gap-1 text-xs font-semibold text-slate-400"><CalendarDays className="h-3.5 w-3.5" /> {item.dateApplied}</p>}</div><select value={item.status} onChange={(event) => update(item.id, 'status', event.target.value)} className={inputClass}>{statuses.map((status) => <option key={status}>{status}</option>)}</select><input value={item.notes || ''} onChange={(event) => update(item.id, 'notes', event.target.value)} placeholder="Notes" className={inputClass} /><div className="flex items-center gap-2"><button type="button" onClick={() => remove(item.id)} className="rounded-xl p-2 text-slate-400 hover:bg-red-50 hover:text-red-500" aria-label="Delete application"><Trash2 className="h-4 w-4" /></button>{item.jobUrl && <a href={item.jobUrl} target="_blank" rel="noreferrer" className="rounded-xl p-2 text-slate-400 hover:bg-orange-50 hover:text-orange-600" aria-label="Open job URL"><ExternalLink className="h-4 w-4" /></a>}</div></div></article>) : <div className="rounded-3xl border border-dashed border-slate-300 p-10 text-center"><p className="font-black text-slate-900">No applications in this view.</p><p className="mt-2 text-sm text-slate-500">Add your first application above to start building a simple job-search log.</p></div>}</section></main></div>;
};

export default Applications;
